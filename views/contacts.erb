<p style="color: red"><%= @error %></p>

<p><strong>Вы можете отправить нам сообщение через контактную форму:</strong></p>

<form action="/contacts" method="POST">

  
  <div class="form-group">
    <label for="exampleInputEmail1">Ваш email</label>
    <input name="contact[name]" type="email" class="form-control" placeholder="Ваш email" value="<%= @c.name %>">
    <small id="emailHelp" class="form-text text-muted">We'll never share your Email with anyone else.</small>
  </div>

  
  <div class="form-group">
    <label for="exampleFormControlTextarea1">Ваше сообщение!</label>
    <textarea name="contact[comment]" type="text" class="form-control" id="exampleFormControlTextarea1" rows="3"><%= @c.comment %></textarea>
  
    <small id="emailHelp" class="form-text text-muted">We'll never share your Message with anyone else.</small>
  </div>

  <p><button type="submit" class="btn btn-primary">Отправить</button></p>

</form>